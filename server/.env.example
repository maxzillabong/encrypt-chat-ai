# Sage Proxy Server Environment Variables

# Port to run the proxy server
PORT=3100

# Shared encryption secret (must match client)
ENCRYPT_CHAT_SECRET=change-me-in-production

# Claude API configuration
# For direct Anthropic: use x-api-key (default)
# For ccproxy: use bearer
ANTHROPIC_API_KEY=your-api-key-or-ccproxy-token
CLAUDE_API_URL=https://api.anthropic.com
AUTH_TYPE=x-api-key

# Qdrant vector database
QDRANT_URL=http://qdrant:6333
